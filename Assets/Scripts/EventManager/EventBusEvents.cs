public class EventBusEvents
{
    public enum EventName
    {
        SPACE_KEY,
        MOVEMENT_KEYS,
        SHIFT_KEY,
        G_KEY,
        E_KEY,
        F_KEY,
        DEATH_EVENT,
        DIALOGUE_EVENT,
    }
}